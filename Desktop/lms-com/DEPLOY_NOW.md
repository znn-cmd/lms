# 🚀 ГОТОВО К ДЕПЛОЮ НА VERCEL!

Все изменения закоммичены и запушены в GitHub. Теперь выполните следующие шаги:

## ✅ Шаг 1: Создайте базу данных (5 минут)

### Рекомендуется Supabase (бесплатно):

1. Перейдите на https://supabase.com
2. Нажмите "Start your project" → "New Project"
3. Заполните:
   - **Name**: lms-com (или любое другое)
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший
4. Нажмите "Create new project"
5. Дождитесь создания проекта (1-2 минуты)
6. Перейдите в **Settings** → **Database**
7. Найдите **Connection string** → **URI**
8. Скопируйте строку (она выглядит так: `postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres`)
9. **Сохраните эту строку** - она понадобится на следующем шаге

## ✅ Шаг 2: Деплой на Vercel (5 минут)

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Перейдите на https://vercel.com
2. Нажмите **"Sign Up"** или **"Log In"** (можно через GitHub)
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий `lms-com` (или импортируйте его)
5. Нажмите **"Import"**

6. **Настройте проект:**
   - Framework Preset: **Next.js** (определится автоматически)
   - Root Directory: `./` (оставьте по умолчанию)
   - Build Command: `prisma generate && npm run build` (уже настроено)
   - Output Directory: `.next` (оставьте по умолчанию)

7. **Добавьте переменные окружения:**
   Нажмите на **"Environment Variables"** и добавьте:

   ```
   DATABASE_URL = [вставьте строку подключения из Supabase]
   ```
   
   Для генерации NEXTAUTH_SECRET откройте терминал и выполните:
   ```bash
   openssl rand -base64 32
   ```
   Или используйте онлайн генератор: https://generate-secret.vercel.app/32
   
   Затем добавьте:
   ```
   NEXTAUTH_SECRET = [сгенерированный секрет]
   NEXTAUTH_URL = https://your-app-name.vercel.app
   ```
   ⚠️ **NEXTAUTH_URL** обновите после первого деплоя!

8. Нажмите **"Deploy"**

9. Дождитесь завершения деплоя (2-3 минуты)

10. После деплоя Vercel покажет вам URL (например: `https://lms-com-xyz.vercel.app`)

11. **Обновите NEXTAUTH_URL:**
    - Перейдите в Settings → Environment Variables
    - Найдите `NEXTAUTH_URL`
    - Измените на реальный URL вашего приложения
    - Нажмите "Redeploy" или подождите автоматического деплоя

### Вариант B: Через CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Добавьте переменные окружения
vercel env add DATABASE_URL
# Вставьте строку подключения из Supabase

vercel env add NEXTAUTH_SECRET
# Вставьте сгенерированный секрет

vercel env add NEXTAUTH_URL
# Вставьте URL после первого деплоя (например: https://lms-com-xyz.vercel.app)
```

## ✅ Шаг 3: Настройка базы данных (2 минуты)

После успешного деплоя нужно применить схему Prisma:

### Вариант A: Через терминал локально

```bash
# Установите переменную окружения
$env:DATABASE_URL="[ваша строка подключения из Supabase]"

# Примените схему
npx prisma db push

# Заполните тестовыми данными (опционально)
npm run db:seed
```

### Вариант B: Через Prisma Studio

```bash
# Установите переменную окружения
$env:DATABASE_URL="[ваша строка подключения из Supabase]"

# Откройте Prisma Studio
npx prisma studio
```

В Prisma Studio вы можете:
- Просмотреть структуру базы данных
- Добавить данные вручную
- Проверить, что все таблицы созданы

### Вариант C: Через Supabase SQL Editor

1. Перейдите в Supabase → SQL Editor
2. Выполните команды из `prisma/schema.prisma` (но лучше использовать Prisma)

## ✅ Шаг 4: Проверка (1 минута)

1. Откройте URL вашего приложения на Vercel
2. Попробуйте зарегистрироваться или войти
3. Проверьте, что интерфейс работает

## 🎉 Готово!

Ваше приложение развернуто на Vercel!

**Демо-аккаунты** (после выполнения `npm run db:seed`):
- **Admin**: `admin@demo.com` / `demo123`
- **HR**: `hr@demo.com` / `demo123`
- **Mentor**: `mentor@demo.com` / `demo123`
- **Candidate**: `candidate@demo.com` / `demo123`

## 🔧 Решение проблем

### Ошибка "Prisma Client not generated"
- Убедитесь, что в `vercel.json` buildCommand включает `prisma generate`
- Проверьте логи деплоя в Vercel Dashboard

### Ошибка подключения к БД
- Проверьте, что `DATABASE_URL` правильный
- Убедитесь, что база данных доступна из интернета
- В Supabase проверьте Settings → Database → Connection pooling

### NextAuth ошибки
- Убедитесь, что `NEXTAUTH_URL` соответствует реальному URL
- Проверьте, что `NEXTAUTH_SECRET` установлен

### База данных пустая
- Выполните `npx prisma db push` с правильным `DATABASE_URL`
- Затем выполните `npm run db:seed` для тестовых данных

## 📞 Нужна помощь?

- Проверьте логи в Vercel Dashboard → Deployments → [ваш деплой] → Logs
- Проверьте документацию: `DEPLOY.md` или `VERCEL_QUICK_START.md`

---

**Все готово к деплою! Удачи! 🚀**

