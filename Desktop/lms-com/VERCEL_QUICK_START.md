# 🚀 Быстрый деплой на Vercel

## Шаг 1: Подготовка кода

```bash
# Добавьте все изменения
git add .

# Закоммитьте
git commit -m "Prepare for Vercel deployment with i18n"

# Запушьте в GitHub
git push origin main
```

## Шаг 2: Создайте базу данных

Рекомендуется **Supabase** (бесплатно):
1. Перейдите на https://supabase.com
2. Создайте новый проект
3. В Settings → Database скопируйте **Connection string** (URI)
4. Замените `[YOUR-PASSWORD]` на ваш пароль

## Шаг 3: Деплой на Vercel

### Через веб-интерфейс:

1. **Импортируйте проект:**
   - Перейдите на https://vercel.com
   - Нажмите "Add New Project"
   - Импортируйте ваш GitHub репозиторий

2. **Настройте переменные окружения:**
   В разделе "Environment Variables" добавьте:
   
   ```
   DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   NEXTAUTH_SECRET=[сгенерируйте через: openssl rand -base64 32]
   NEXTAUTH_URL=https://your-app.vercel.app
   ```
   
   ⚠️ **NEXTAUTH_URL** обновите после первого деплоя!

3. **Нажмите "Deploy"**

### Через CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel

# Добавьте переменные окружения
vercel env add DATABASE_URL
vercel env add NEXTAUTH_SECRET
vercel env add NEXTAUTH_URL
```

## Шаг 4: Настройка базы данных

После успешного деплоя:

```bash
# Примените схему Prisma
npx prisma db push --schema=./prisma/schema.prisma

# Заполните тестовыми данными (опционально)
npm run db:seed
```

Или через Prisma Studio:
```bash
npx prisma studio
```

## Шаг 5: Обновите NEXTAUTH_URL

После деплоя Vercel даст вам URL (например: `https://lms-com.vercel.app`)

1. В Vercel Dashboard → Settings → Environment Variables
2. Обновите `NEXTAUTH_URL` на ваш реальный URL
3. Передеплойте проект (или подождите автоматического деплоя)

## ✅ Готово!

Ваше приложение должно быть доступно по адресу, который предоставил Vercel.

**Демо-аккаунты** (после seed):
- Admin: `admin@demo.com` / `demo123`
- HR: `hr@demo.com` / `demo123`
- Mentor: `mentor@demo.com` / `demo123`
- Candidate: `candidate@demo.com` / `demo123`

## 🔧 Решение проблем

### Ошибка "Prisma Client not generated"
- Проверьте, что в `vercel.json` buildCommand включает `prisma generate`
- Убедитесь, что в `package.json` есть `"postinstall": "prisma generate"`

### Ошибка подключения к БД
- Проверьте, что `DATABASE_URL` правильный
- Убедитесь, что база данных доступна из интернета
- Проверьте настройки firewall в Supabase

### NextAuth ошибки
- Убедитесь, что `NEXTAUTH_URL` соответствует реальному URL
- Проверьте, что `NEXTAUTH_SECRET` установлен

## 📚 Подробная инструкция

См. `DEPLOY.md` для более детальной информации.

