# Быстрая настройка базы данных

## Самый простой способ - Supabase (2 минуты)

1. Перейдите на https://supabase.com
2. Нажмите "Start your project" и зарегистрируйтесь
3. Создайте новый проект (выберите регион, придумайте пароль)
4. Дождитесь создания проекта (~2 минуты)
5. Перейдите в Settings → Database
6. Найдите "Connection string" → "URI"
7. Скопируйте строку подключения
8. Откройте файл `.env` в проекте
9. Вставьте скопированную строку в `DATABASE_URL=`
10. Запустите:

```bash
npm run db:push
npm run db:seed
```

Готово! 🎉

## Альтернатива - Docker (если установлен Docker)

```bash
# Запустить PostgreSQL
docker run --name lms-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=lms_com -p 5432:5432 -d postgres

# В .env файле укажите:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lms_com?schema=public"

# Применить схему
npm run db:push
npm run db:seed
```

## Если PostgreSQL уже установлен локально

1. Откройте `.env` файл
2. Укажите правильные credentials:

```env
DATABASE_URL="postgresql://ВАШ_ПОЛЬЗОВАТЕЛЬ:ВАШ_ПАРОЛЬ@localhost:5432/lms_com?schema=public"
```

3. Создайте базу данных (если еще не создана):

```bash
# Через psql
psql -U postgres
CREATE DATABASE lms_com;
\q

# Или через pgAdmin - создайте базу данных lms_com
```

4. Запустите:
```bash
npm run db:push
npm run db:seed
```

