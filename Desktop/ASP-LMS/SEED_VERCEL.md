# Как заполнить базу данных на Vercel

## Проблема
После деплоя на Vercel база данных пустая, поэтому логины не работают.

## Решение: Использовать API endpoint для seed

### Шаг 1: Вызовите API endpoint

Откройте в браузере или выполните curl:

```
https://your-app.vercel.app/api/admin/seed?token=seed-me-please
```

Или через curl:
```bash
curl -X POST "https://your-app.vercel.app/api/admin/seed?token=seed-me-please"
```

### Шаг 2: Проверьте результат

Вы должны увидеть JSON ответ:
```json
{
  "success": true,
  "message": "Database seeded successfully",
  "users": {
    "student": "user",
    "hr": "hr"
  },
  "course": "Основы работы риэлтора..."
}
```

### Шаг 3: Попробуйте войти

После успешного seed попробуйте войти:
- **Student:** `user` / `user`
- **HR:** `hr` / `hr`

## Безопасность

⚠️ **Важно:** После заполнения базы данных рекомендуется:
1. Удалить или защитить этот endpoint
2. Или изменить токен в переменных окружения Vercel:
   - Зайдите в Vercel Dashboard → Settings → Environment Variables
   - Добавьте: `SEED_TOKEN` = `ваш-секретный-токен`
   - Обновите URL: `?token=ваш-секретный-токен`

## Альтернативный способ: Через Vercel CLI

```bash
# 1. Получите переменные окружения
vercel env pull .env.local

# 2. Выполните seed локально (подключится к Vercel базе)
npm run db:seed
```

Но API endpoint проще и быстрее! 🚀

